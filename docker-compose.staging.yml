services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio-staging:latest
    ports:
      - "3000:3000"
    restart: always
    environment:
      - NODE_ENV=production
      - BASE_URL=moe.thedev.id

